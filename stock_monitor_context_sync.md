# 外資成本監控系統 - 跨裝置銜接紀錄 (Context Sync)

此文件建立於 2026-04-27，旨在協助使用者在不同電腦上繼續開發進度。

## 1. 專案現況總結
目前專案位於 `OneDrive\Desktop\測試` 資料夾中，包含以下核心組件：
- **XQ 選股腳本 (`外資成本溢價篩選.xs`)**：已完成。篩選條件為外資成本的 1.04 倍至 1.10 倍之間。
- **Node.js 監控儀表板 (`server.js`)**：已完成。具備 WebSocket 即時更新功能，會自動偵測「發動結果」資料夾內的 CSV 檔案。
- **啟動工具 (`啟動儀表板.bat`)**：用於快速啟動 Node.js 伺服器並自動開啟網頁。

## 2. 關鍵決策與開發狀態
目前處於**自動化方案抉擇階段**（詳見 `開發與未來選項紀錄.md`）：
- **方案一 (UI Automation)**：使用 Python 模擬滑鼠操作 XQ。優點是完全保留現有腳本，缺點是電腦不能鎖定/睡眠。
- **方案二 (雲端 Python 爬蟲)**：直接抓取證交所資料計算成本。優點是可置於 GitHub Actions 免費運行且不需開機，缺點是與 XQ 數據可能略有微小誤差且需重寫邏輯。

## 3. 歷史對話摘要 (Conversation History)

### 對話 f3310794: 專案檔案讀取與環境確認
- 確認了使用者已安裝 Node.js。
- 修正了 `server.js` 的路徑問題，使其能動態抓取 OneDrive 桌面下的資料夾。
- 測試並確認 `啟動儀表板.bat` 運行正常。

### 對話 2e739c5b / 108e1015: XQ 腳本優化
- 確立了「1.04x 觸發邏輯」與「過濾溢價過高 (>15% 或 10%)」的原則。
- 整合了儀表板顯像、警示與選股功能。

### 對話 5a57f21b: 系統初始開發
- 將原本 Python OCR 的手動流程轉化為原生 XQ (XS) 腳本。
- 目標是自動化追蹤外資 60 日加權平均成本附近的股票。

## 4. 如何在另一台電腦銜接
1. **程式碼衔接**：由於專案存放在 OneDrive，只要在另一台電腦登入同一帳號，程式碼會自動同步。
2. **AI 背景銜接**：
   - 如果您希望在另一台電腦的 AI 介面中看到完整的「聊天氣泡」歷史，請將本機路徑 `%AppData%\.gemini\antigravity` (即 `C:\Users\phili\.gemini\antigravity`) 的完整內容複製到新電腦的相同位置。
   - 如果您是開啟新的對話，**請直接將本文件 (stock_monitor_context_sync.md) 內容貼給 AI**，或是請 AI 讀取此檔案，它就能立刻掌握所有開發背景與目前的決擇點。

---
*備註：此文件由 Antigravity 助手生成，用於協助您順利跨裝置遷移。*
